#!/bin/sh
set -eu

branch="$(git symbolic-ref --quiet --short HEAD || true)"

case "$branch" in
  main|develop)
    echo "Direct commits to $branch are blocked. Create a working branch first." >&2
    exit 1
    ;;
esac
