{
  "id": "home-assistant",
  "name": "Home Assistant",
  "version": "0.1.0",
  "entrypoint": "./src/index.ts",
  "capabilities": [
    {
      "id": "home-assistant.entities",
      "title": "Discover Home Assistant entities",
      "description": "Reads Home Assistant entity state and metadata for routing and resolution.",
      "actions": ["discover-entities", "read-state"],
      "examples": ["what Home Assistant entities are available?", "are the kitchen lights on?"]
    },
    {
      "id": "home-assistant.control",
      "title": "Control Home Assistant entities",
      "description": "Calls supported Home Assistant services for lights, switches, scenes, scripts, and climate entities.",
      "actions": [
        "turn-on",
        "turn-off",
        "switch-on",
        "switch-off",
        "activate-scene",
        "run-script",
        "set-temperature"
      ],
      "examples": [
        "turn on the lights",
        "turn on the living room lights",
        "turn off the kitchen lights",
        "set the thermostat to 70"
      ]
    }
  ],
  "configurationSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["baseUrl"],
    "properties": {
      "baseUrl": {
        "type": "string",
        "format": "uri",
        "description": "Base URL for the Home Assistant instance, for example http://homeassistant.local:8123."
      },
      "accessToken": {
        "type": "string",
        "minLength": 1,
        "description": "Long-lived Home Assistant access token."
      },
      "accessTokenRef": {
        "type": "string",
        "minLength": 1,
        "description": "Reference to a Brain-managed secret containing the Home Assistant access token."
      }
    }
  },
  "safetyDefaults": {
    "confirmation": "not-required",
    "disabledActions": []
  }
}
